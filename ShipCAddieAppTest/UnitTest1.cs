using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ShipCAddieAppTest
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]

        //<Returns> 
        //Return token based on consumer username and password. The token is needed to access all endpoints into this integration package.
        public void GetToken()
        {
        }
    }
}
